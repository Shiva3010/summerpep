#include <iostream>
using namespace std;

int main()
{
    string binary ;
    cin >> binary;

    for (int i = 0; i<binary.length(); i++)
    {
        for (int j = i+1; j<= binary.length-1(); j++)
        {
            if binary[i] == 0;
            {
                [i]<<[j];
            }
        }   
    }
    return 0;
}